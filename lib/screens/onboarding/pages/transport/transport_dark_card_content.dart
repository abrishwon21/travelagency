import 'package:flutter/material.dart';

import '../../../../constants.dart';

class TransportDarkCardContent extends StatelessWidget {
  const TransportDarkCardContent();

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.airplanemode_active_rounded,
      color: kWhite,
      size: 96.0,
    );
  }
}
